# --- LiveKit Core ---
livekit-agents>=1.4.1
livekit-api>=0.7.1
livekit-protocol>=0.10.0

# --- Plugins ---
# These plugins are wrappers that send requests to your Docker containers
livekit-plugins-openai>=1.4.1
livekit-plugins-silero>=1.4.1

# --- Utilities ---
python-dotenv>=1.0.0
aiohttp>=3.9.0
certifi
numpy

# --- Auto-Dialer Frontend ---
streamlit>=1.30.0
pandas>=2.0.0
openpyxl>=3.1.0